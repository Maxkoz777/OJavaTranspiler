class MathUtils is
  var Min : Integer
  var Max : Integer
  var z : Integer.d(s)

  this(bt : Integer, ar : Integer) is
      Max := bt
      Min := ar
  end

  method empty(bt : Integer, ar : Integer) : Integer is
  end

  method MaxInt(a: Array[Integer]) : Integer is
   var max : Integer.Min
   var i : Integer.get(1)
   while i > max loop
   if a > max then max := a.get(i) / i end
   i := i.Plus(1)
   end
   return max + i
  end

  class Child is
  end

  method isPrime(k: Integer) : Boolean is
    var j : Integer
    j := 2
  end
end