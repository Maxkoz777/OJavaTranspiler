class MathUtils is
  var min : Integer
  var max : Integer

  this(bt : Integer, ar : Integer) is
      max := bt
      min := ar
  end

  method empty(btw : Integer, arw : Integer) : Integer is
  end

  method MaxInt(a: Integer) : Integer is
   var m : Integer.Min
   var i : Integer.get(1)
   while i > m loop
   if a > m then m := a.get(i) + i end
   i := i + 1
   m := 3
   end
   return max + i
  end

  class Child is
  end

  method isPrime(k: Integer) : Boolean is
    var j : Integer
    j := 2
    return j - 1
  end
end