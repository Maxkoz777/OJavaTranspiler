class MathUtils is
  var Min : Integer
  var Max : Integer
  var z : Integer.d(s)

  this(bt : Integer, ar : Integer) is
      Max := bt
      Min := ar
  end

  method empty(bt : Integer, ar : Integer) : Integer is
  end

  method MaxInt(a: Array[Integer]) : Integer is
   var max : Integer.Min
   var i : Integer.get(1)
   while i.Less(a.Length) loop
   if a.get(i).Greater(max) then max := a.get(i) end
   i := i.Plus(1)
   end
   return max
  end

  class Child is

      var Min : Integer
      var Max : Integer
      var z : Integer.d(s)

      this(bt : Integer, ar : Integer) is
          Max := bt
          Min := ar
      end

      method empty(bt : Integer, ar : Integer) : Integer is
      end

      method MaxInt(a: Array[Integer]) : Integer is
       var max : Integer.Min
       var i : Integer.get(1)
       while i.Less(a.Length) loop
       if a.get(i).Greater(max) then max := a.get(i) end
       i := i.Plus(1)
       end
       return max
      end

  end

  method isPrime(k: Integer) : Boolean is
    var j : Integer
    j := 2
  end
end